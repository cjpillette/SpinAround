<%= form_for(composition) do |f| %>
  <% if composition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(composition.errors.count, "error") %> prohibited this composition from being saved:</h2>

      <ul>
      <% composition.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fibre_type_id %>
    <%= f.text_field :fibre_type_id %>
  </div>

  <div class="field">
    <%= f.label :percent_content %>
    <%= f.number_field :percent_content %>
  </div>

  <div class="field">
    <%= f.label :yarn_id %>
    <%= f.text_field :yarn_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
