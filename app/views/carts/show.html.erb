<p id="notice"><%= notice %></p>

<h2>Your shopping Cart</h2>
<ul>
  <% @cart.line_items.each do |item| %>
  <li><%= item.quantity %> skein of <%= item.skein.name %></li>
  <% end %>
</ul>

<%= button_to 'Empty cart', @cart, method: :delete, data: { confirm: 'Are you sure?' } %>
